[package]
name = "xtask"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
walkdir = "2.5"
fs_extra = "1.3"
toml = "0.9"
syn = { version = "2.0", features = ["full", "visit"] } # Added for parsing source code
